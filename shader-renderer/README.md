# Shader renderer

Headless renderer that take JSON RPC command, receive compiled shader (SPIRV, DXIL, WGSL), render a shader pipeline and return its output. This is used as a shader toy with the VSCode extension shader-validator to visualize shader result.